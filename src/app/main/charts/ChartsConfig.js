import Charts from "./Charts";

const ChartConfig = {
  settings: {
    layout: {
      config: {},
    },
  },
  routes: [
    {
      path: "page2",
      element: <Charts />,
    },
  ],
};

export default ChartConfig;
