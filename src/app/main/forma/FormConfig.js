import Form from "./Form";

const FormConfig = {
  settings: {
    layout: {
      config: {},
    },
  },
  routes: [
    {
      path: "page3",
      element: <Form />,
    },
  ],
};

export default FormConfig;
